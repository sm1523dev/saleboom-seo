# Copy this to .env.local and fill in the values.
# Run: cp .env.local.example .env.local

# ── Auth ──────────────────────────────────────────────────────────────────────
# For local dev: AUTH_PROVIDER=mock skips all auth (auto-logged in)
AUTH_PROVIDER=mock

# For production with Microsoft Entra ID:
# AUTH_PROVIDER=authjs
# AUTH_SECRET=                     # openssl rand -base64 32
# AUTH_ENTRA_ID_ID=                # Application (client) ID from Azure portal
# AUTH_ENTRA_ID_SECRET=            # Client secret value
# AUTH_ENTRA_ID_ISSUER=            # https://login.microsoftonline.com/<tenant-id>/v2.0

# ── Database ──────────────────────────────────────────────────────────────────
# Docker Compose (default):
DATABASE_URL=postgresql://postgres:root@localhost:5432/saleboom_seo

# ── Queue ─────────────────────────────────────────────────────────────────────
QUEUE_PROVIDER=bullmq
REDIS_URL=redis://localhost:6379

# ── Crawl ─────────────────────────────────────────────────────────────────────
CRAWL_PROVIDER=firecrawl
FIRECRAWL_API_KEY=                 # From firecrawl.dev (cloud) or any string (self-hosted)
# FIRECRAWL_API_URL=http://localhost:3002   # Uncomment for self-hosted Firecrawl

# ── AI ────────────────────────────────────────────────────────────────────────
# groq = free Groq API for SEO copy suggestions (recommended)
# mock = zero-config stub responses
AI_PROVIDER=groq

# For production with Azure OpenAI:
# AI_PROVIDER=azure
# AZURE_OPENAI_ENDPOINT=           # https://<resource>.openai.azure.com/
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_DEPLOYMENT=         # e.g. gpt-4o

# ── Logging ───────────────────────────────────────────────────────────────────
LOG_PROVIDER=console               # console (dev) | pino (prod)
# LOG_LEVEL=info                   # debug | info | warn | error
# LOG_FILE=                        # Set per-process in npm scripts (see package.json)

# ── Observability ─────────────────────────────────────────────────────────────
# SENTRY_DSN=                      # From sentry.io project settings
# NEXT_PUBLIC_SENTRY_DSN=          # Same value — exposed to browser for client errors

# OpenTelemetry (optional — stdout JSON used in dev when not set)
# OTEL_EXPORTER_OTLP_ENDPOINT=     # e.g. https://otel.example.com/v1/traces
# OTEL_SERVICE_NAME=saleboomseo    # Override service name in traces

# Uptime monitoring (external ping service — see docs/uptime-monitoring.md)
# UPTIME_MONITOR_URL=              # Dashboard URL of your monitor (for reference only)

# Alerts — set at least one to receive failure/spike notifications
# SLACK_ALERT_WEBHOOK=             # Slack incoming webhook URL
# ALERT_EMAIL_TO=                  # Comma-separated admin emails for alert delivery

# ── AEO Provider Keys (Signal 1 — prompt sampling) ───────────────────────────
# Platform-managed — users never see or configure these.
# Groq: free tier at https://console.groq.com (covers 3 of 4 providers)
# Google: free tier at https://aistudio.google.com/apikey
# GROQ_API_KEY=                      # Powers Llama 3.3, Mixtral, Meta AI slots
# GOOGLE_AI_API_KEY=                 # Powers Gemini slot
# NVIDIA_NIM_API_KEY=                # AEO Asian AI family — build.nvidia.com

# ── App ───────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
